---
name: Bug report
about: Something doesn't run or produces wrong output
labels: bug
---

**What happened**

**Command you ran** (e.g. `python -m strategies.trend --paper --instruments SPY,QQQ`)

**Full output / traceback**

```
paste here
```

**Environment**
- OS:
- Python version:
- Install method: `pip install -e .` (core) / `pip install -r requirements.txt` (full)
- API keys configured? (never paste keys, yes/no per provider)

**Book reference (if applicable)**: chapter/section this relates to
